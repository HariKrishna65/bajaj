services:
  - type: web
    name: hackrx-bill-extractor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    runtime: python;version=3.10.13
    envVars:
      - key: GEMINI_API_KEY
        sync: false
